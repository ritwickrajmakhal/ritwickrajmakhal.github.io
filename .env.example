
# SMTP configuration (fill with your SMTP provider credentials)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
OWNER_EMAIL=

# Vercel Postgres / DATABASE URL (example):
DATABASE_URL=
REACT_APP_CONTACT_ENDPOINT=